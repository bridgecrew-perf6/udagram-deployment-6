# Initialize a folder for the deployment

1. run `eb init` to initialize the project folder.
2. Select a default region.
3. Select an application to use, or create a new one.
4. Select a platform branch `(NodeJS)`.